apiVersion: v1
kind: Service
metadata:
  labels:
    monitored: "true"
  name: api-gateway-service
  namespace: app
spec:
  selector:
    app: api-gateway
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP