apiVersion: v1
kind: Service
metadata:
  labels:
    monitored: "true"
  name: anamoly-service
  namespace: app
spec:
  selector:
    app: anamoly-service
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP