apiVersion: v1
kind: Service
metadata:
  labels:
    monitored: "true"
  name: auth-service
  namespace: app
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP