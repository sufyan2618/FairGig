apiVersion: v1
kind: Service
metadata:
  labels:
    monitored: "true"
  name: mongodb-service
  namespace: app
spec:
  selector:
    app: mongodb
  ports:
    - name: http
      port: 27017
      targetPort: 27017
  type: ClusterIP