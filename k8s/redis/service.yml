apiVersion: v1
kind: Service
metadata:
  labels:
    monitored: "true"
  name: redis-service
  namespace: app
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP